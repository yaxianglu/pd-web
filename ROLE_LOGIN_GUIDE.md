# 角色登录指南

## 用户角色和对应页面

### 1. 超级管理员 (super_admin)
- **用户名**: `super_admin`
- **密码**: `S3cur3@dm1n2025!@#`
- **登录后跳转**: `/hospital` (医院管理界面)
- **权限**: 拥有系统最高权限，可以管理所有功能

### 2. 普通管理员 (admin)
- **用户名**: `admin_user`
- **密码**: `Adm1n@2025!@#`
- **登录后跳转**: `/hospital` (医院管理界面)
- **权限**: 拥有管理权限，可以管理医院相关功能

### 3. 经理 (manager)
- **用户名**: `doctor_user`
- **密码**: `D0ct0r@2025!@#`
- **登录后跳转**: `/list` (列表页面)
- **权限**: 拥有管理权限，可以查看和管理列表数据

### 4. 操作员 (operator)
- **用户名**: `sales_user`
- **密码**: `S@l3s@2025!@#`
- **登录后跳转**: `/partners` (合作伙伴页面)
- **权限**: 拥有基本操作权限，可以查看合作伙伴信息

### 5. 医院管理员 (admin)
- **用户名**: `hospital_user`
- **密码**: `H0sp1t@l2025!@#`
- **登录后跳转**: `/hospital` (医院管理界面)
- **权限**: 拥有管理权限，可以管理医院相关功能

### 6. 患者 (patient)
- **登录方式**: 使用UUID登录
- **登录后跳转**: `/patient` (患者页面)
- **权限**: 患者用户，可以查看自己的治疗信息

## 登录测试步骤

### 员工登录测试
1. 访问 `http://localhost:3000/login`
2. 在"工作人員登入"部分输入用户名和密码
3. 点击"登入"按钮
4. 系统会根据用户角色自动跳转到对应页面

### 患者登录测试
1. 访问 `http://localhost:3000/login`
2. 在"患者登入"部分输入有效的UUID
3. 点击"登入"按钮
4. 系统会跳转到患者页面

## 页面功能说明

### `/hospital` - 医院管理界面
- 左侧导航栏：系统菜单和账户管理
- 患者信息卡片：显示当前患者基本信息
- 治疗纪要：预约管理和治疗记录
- 治疗状态：进度跟踪和流程管理
- 患者列表：所有患者的表格视图

### `/list` - 列表页面
- 微笑测试数据列表
- 患者信息管理
- 状态跟踪和操作

### `/partners` - 合作伙伴页面
- 合作伙伴信息管理
- 数据表格和搜索功能
- 详情查看和操作

### `/patient` - 患者页面
- 患者个人信息展示
- 治疗进度跟踪
- 相关文档和记录

## 技术实现

### 角色路由配置
配置文件：`pd-web/src/contants/roleRoutes.js`

```javascript
export const ROLE_ROUTES = {
  super_admin: '/hospital',
  admin: '/hospital',
  manager: '/list',
  operator: '/partners',
  patient: '/patient'
};
```

### 登录逻辑
- 员工登录：根据后端返回的 `role` 字段确定跳转页面
- 患者登录：固定跳转到 `/patient` 页面
- 使用 `getRouteByRole()` 函数统一处理路由映射

### 权限控制
- 所有页面都使用 `ProtectedRoute` 组件包装
- 需要登录后才能访问
- 根据用户类型和角色显示不同内容

## 注意事项

1. **密码加密**: 前端使用SHA256加密密码后发送给后端
2. **Token管理**: 登录成功后保存token到localStorage
3. **会话管理**: 患者UUID保存在sessionStorage中
4. **响应式设计**: 所有页面都支持1125px断点的移动端适配
5. **错误处理**: 登录失败会显示相应的错误信息

## 数据库用户

所有测试用户都已在数据库中创建，密码都已正确加密。如需重新创建用户，请运行：

```sql
-- 删除现有用户
DELETE FROM `pd`.admin_users WHERE username IN ('super_admin', 'admin_user', 'doctor_user', 'sales_user', 'hospital_user');

-- 重新插入用户（使用add_new_users.sql脚本）
```

## 故障排除

### 登录失败
1. 检查用户名和密码是否正确
2. 确认后端服务是否正常运行
3. 检查网络连接
4. 查看浏览器控制台错误信息

### 页面跳转问题
1. 确认路由配置是否正确
2. 检查角色映射是否匹配
3. 验证用户角色信息是否正确返回

### 权限问题
1. 确认用户状态是否为 `active`
2. 检查用户是否被锁定
3. 验证token是否有效 